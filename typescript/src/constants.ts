export enum GAME_STATE {
    notStarted = -1,
    started = 0,
    ended = 1
}

export enum ACTION {
    up = 'up',
    down = 'down',
    left = 'left',
    right = 'right',
    stay = 'stay',
    bomb = 'bomb'
}